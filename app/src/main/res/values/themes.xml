<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChallengeChapter2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ChallengeChapter2" parent="Base.Theme.ChallengeChapter2" />

    <style name="EditText" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="boxStrokeColor">@color/white</item>
        <item name="android:textSize">40sp</item>
        <item name="android:background">@color/white</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:fontFamily">@font/circular_bold</item>
        <item name="android:textColorHint">#999999</item>
    </style>
</resources>